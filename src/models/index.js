const seasonModel = require('./season');
const chapterModel = require('./chapter');
const rotatingImagePairModel = require('./rotatingImagePair');

module.exports = {
    seasonModel,
    chapterModel,
    rotatingImagePairModel
};